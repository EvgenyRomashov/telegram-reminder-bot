version: '3.8'

services:
  reminder_bot:
    build: .
    container_name: telegram_reminder_bot
    env_file:
      - .env
    volumes:
      - ./reminders.db:/app/reminders.db # Persist the SQLite database
    restart: always
